<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

        <record id="password_recovery_email_template" model="mail.template">
            <field name="name">GYM: Pass recovery</field>
            <field name="model_id" ref="model_res_partner"/>
            <field name="email_from">${('&lt;%s&gt;' % (object.env.user.company_id.email or user.email)) | safe}</field>
            <field name="email_to">${object.email}</field>
            <field name="subject">GYM: Password recovery</field>
            <field name="body_html" type="html">
                <h1>Hola</h1>
            </field>
        </record>
    
    </data>

</odoo>
